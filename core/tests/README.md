# VectorForge Unit Tests

Contains the test suite for core vector data structures and algorithms.

## Running Tests

```bash
# Via CTest
ctest --test-dir ../../build --output-on-failure

# Direct binary
../../build/core/vectorforge_test
```
